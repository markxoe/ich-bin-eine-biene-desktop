name: Build for Windows

on: push

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      
      - name: Install
        run: npm i

      - name: Build
        run: npm run build-win
